extends ../layouts/main-layout.pug

block styles
    link(rel="stylesheet" href="/css/forms.css")

block content
    form(action="/admin/add-sub-category" method="POST")
        input(type="hidden" name="_csrf" value=`${csrfToken}`)
        .form-group.row
            .col-sm-12
                h2.mb-3 En Alt Kategori Ekleme
                    hr
            .col-sm-12
                select(class="browser-default custom-select custom-select-lg mb-3" name="categoryids" aria-label="Kategori Seçme" onchange="location = this.value;")
                    option.selected Ana Kategori Seç
                    each category in categories
                        option(value=`/admin/add-sub-sub-category-2/${category._id}`) #{category.name}

        //- .form-group.row
        //-     .col-sm-10.offset-sm-2
        //-         button(type="submit" data-toggle="tooltip" data-placement="top" title="Kategori Ekle").btn.btn-success
        //-             <i class="fas fa-plus"></i>